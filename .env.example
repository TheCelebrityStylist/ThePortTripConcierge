# Dev-only UI unlock for planner premium actions.
# Honored in non-production when set true.
NEXT_PUBLIC_DEV_UNLOCK=false

# Local/preview bypass for paywall testing without production billing.
NEXT_PUBLIC_DEV_BYPASS_PAYWALL=false
